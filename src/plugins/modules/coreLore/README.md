# 设定库

官方重型模块：人物 / 世界 / 势力等档案的集中管理。侧边栏为设定库文件树，主视图提供力导向图与网格两种布局的卡片编辑区。

## 能力

- **文件树**：设定库条目按树状组织，随工程保存
- **双布局编辑区**：力导向图（关系可视化）与网格卡片（批量编辑）
- **贡献点**：`sidebar.views`（设定库侧栏）

## 兼容性

- 设定数据随 `.writeproj` 保存，与 `formatVersion` 绑定
- 侧边栏变体挂载于活动栏「设定库」，可与其他模块实例并行使用
