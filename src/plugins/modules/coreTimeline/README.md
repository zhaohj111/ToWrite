# 时间轴

官方重型模块：按卷组织故事事件，在水平轴体上拖动事件标签，直观梳理剧情节拍。

## 能力

- **分卷 + 文件树**：事件按卷归档，随工程保存
- **水平轴体**：故事事件标签沿时间轴自由拖动定位
- **贡献点**：`sidebar.views`（时间轴文件侧栏）

## 兼容性

- 时间轴数据随 `.writeproj` 保存，与 `formatVersion` 绑定
- v0.7 将补充更多贡献点（骨架预留）
